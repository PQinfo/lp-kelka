# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Windows metadata
*:Zone.Identifier

# Environment-specific — each server keeps its own
.htaccess


-old.htaccess
# Generated build/test artifacts
.cache/
playwright-report/
test-results/
# Derivados do pipeline de imagens: `npm run images` (e o build) recriam os
# dois a partir dos PNG/WebP versionados, byte a byte. O resultado final ja
# viaja em dist/. Versiona-los seria a mesma imagem tres vezes no repositorio.
src/assets/optimized/
src/generated/

# Secoes aposentadas, mantidas so na maquina. Nenhum codigo ativo as importa.
archive/
.env
.env.*
!.env.example

# Exportacoes originais das laminas da segunda secao. O pipeline le apenas os
# lamina-*.webp ao lado; estes PNG sao a arte bruta, guardada so na maquina.
src/assets/segundasecao/ChatGPT Image*.png
