# Required for release builds: definitive public URL, including subfolder if used.
SITE_URL=
# Optional first-party endpoint. Empty means no telemetry is sent.
VITE_METRICS_ENDPOINT=
VITE_METRICS_SAMPLE_RATE=0.1
